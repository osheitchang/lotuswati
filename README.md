# lotuswati
Internal communication platform for sales teams.

## Backend Required Environment Variables

The backend now fails at startup if either of these is missing:

- `JWT_SECRET`
- `WA_VERIFY_TOKEN`
